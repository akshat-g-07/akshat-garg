const express = require("express");
const router = express.Router();

// login route
router.post("/login", () => {});
// refesh route
router.get("/refresh", () => {});
// signup route
router.post("/signup", () => {});
// logout route
router.post("/logout", () => {});

module.exports = router;
